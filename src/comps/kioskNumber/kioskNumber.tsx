import React, {useContext, useEffect, useState} from "react";

const kioskNumberPage: React.FC<any> = ({kioskNumber}) => {
    return <div className='kioskNumber'>{kioskNumber}</div>
};

export default kioskNumberPage;